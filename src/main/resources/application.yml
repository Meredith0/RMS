spring:
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/rms?serverTimezone=CST&useSSL=false
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
    redis:
        host: 139.196.120.94
        port: 6379
        #连接超时时间(毫秒)
        timeout: 2000
        database: 0

mybatis:
    configuration:
        #    开启驼峰映射
        map-underscore-to-camel-case: true
    mapper-locations: classpath*:mapper/*.xml
